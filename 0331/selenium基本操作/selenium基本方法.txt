浏览器最大化
driver.maximize_window
浏览器最小化
driver.minimize_window
浏览器窗口全屏
driver.fullscreen_window
获取当前浏览器窗口的大小
driver.get_window_size
设置浏览器窗口的大小
set_window_size(x,y)
x:宽
y:高
获取窗口的位置
driver.get_window_position
设置窗口的位置
driver.set_window_position()
获取当前网页的title
driver.title
获取页面的源码
driver.page_source
通过句柄切换当前窗口
driver.current_window_handle
获取所有窗口句柄
driver.window_handles
切换当前窗口
driver.switch_to.window

使用find_element("locator","value")
其中locator:id,name,xpath,css


元素定位，id是首选。


判断元素是否可见
is_displayed，如果可见，返回true,不可见返回false
判断元素是否可用enabled，该元素可用。不可用返回false.
判断元素是否选中，is_selected,选中返回true.

鼠标操作
actionchains.



