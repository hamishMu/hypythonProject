知道了po的思想，借助该思想，我们将页面元素的定位，操作和业务场景
从测试用例中分离出来，从而形成较为健壮的测试用例。
需要对selenium提供的api进行二次封装。
结构：
1.base :用来对selenium 中的api进行封装
2.common:用来放置一些公共的函数或者方法文件。如解析yaml文件，解析csv文件的
3.config:配置测试项目中的配置信息，如系统的ip地址，端口。
4.data:用来放置csv文件，内容是测试用例参数化用到的数据，也可以放其他类型，比如excel文件，json文件。
5.report:用来防止测试执行的报告
6.test:用来测试相关文件，其中pageobject用来存放页面对象，子目录testcase存放测试用例。